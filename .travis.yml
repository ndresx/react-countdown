language: node_js
node_js:
  - "node"

branches:
  only:
    - master

jobs:
  include:
    - stage: "Build"
      script: yarn run build
    - stage: "Coverage"
      script: cat ./coverage/lcov.info | node_modules/.bin/coveralls --verbose
      script: rm -rf ./coverage
